# 変更履歴

このファイルには公開後のリリースを記録します。
公開前(v0.1 〜 v1.43)の開発経緯は [docs/DESIGN.md](docs/DESIGN.md) の「ロードマップ」節に
バージョンごとの判断理由つきで残してあります。

## v1.43.0 — 初回公開(2026-08-19)

DVM を GPL-3.0-or-later で公開しました。この時点で入っている機能は次のとおりです。

### 管理

- 動画を**コピーも移動もせず**、元の場所に置いたまま SQLite で管理
- フォルダ監視による自動取り込み、移動・リネームの追従(先頭 1MB + サイズのハッシュ)
- 外付け HDD / NAS 対応。ドライブ未接続を「ファイル消失」と誤判定しない
- 複数ライブラリの切り替え。ライブラリごと外付け HDD に置いて別 PC へ持ち運べる
- 重複検出、見つからないファイルの検出と再リンク、ごみ箱経由の削除
- 操作履歴と取り消し、DB の自動バックアップと復元

### 分類・検索

- タグ / タググループ / シリーズ / 評価 / スマートフォルダ / プレイリスト
- 空白区切り AND 検索、パス検索、詳細検索(解像度・尺・コーデック・追加日など)
- フォルダーツリーからの絞り込み、一覧の列選択と並べ替え、矩形選択

### 再生・表示

- libmpv 埋め込みによるアプリ内再生(ほぼ全フォーマットを変換なしで)
- ホバープレビュー、レジューム、視聴履歴、チャプター、HDR パススルー、字幕の見た目設定
- 数万件でも軽いサムネイルグリッド(仮想化)

### AI 連携

- MCP サーバー同梱。Claude Desktop / Claude Code などから検索・統計・タグ付け・整理ができる
  (**既定は読み取り専用**)
- アプリ内 AI アシスタント。Anthropic / OpenAI / Gemini / OpenAI 互換(ローカル LLM 含む)に対応

### 既知の制限

- Windows 専用
- コード署名なし。インストーラ実行時に SmartScreen の警告が出る
- AI の API キーは `app.db` に平文で保存される(共用 PC では使わないでください)
- 自動更新なし。新しい版は Releases から入れ直す
